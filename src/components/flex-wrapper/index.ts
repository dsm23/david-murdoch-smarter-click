import styled from 'styled-components';

const FlexWrapper = styled('div')`
  display: -webkit-box;
  display: -ms-flexbox;
  display: flex;
  margin-top: 104px;
`;

export { FlexWrapper };
export default FlexWrapper;
